# hishtory Configuration
# Copy this file to ~/.envs/hishtory.env and configure

# Self-hosted hishtory server URL (optional)
# Without this, hishtory runs in local-only mode
# HISHTORY_SERVER=https://hishtory.example.com

# Secret key for syncing across devices (optional)
# Get this from `hishtory status` after first init
# Or use the same key on all devices to sync history
# HISHTORY_SECRET=your-secret-key-uuid
