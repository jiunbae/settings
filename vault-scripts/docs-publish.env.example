# Copy to ~/.envs/docs-publish.env and fill in.
# The host and account are deployment details, so they live here rather than in
# this repository, which is public.

DOCS_HOST=       # docs server hostname or address
DOCS_USER=       # ssh account on that host
DOCS_ROOT=/var/www/docs
DOCS_URL=https://docs.jiun.dev
