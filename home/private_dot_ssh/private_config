# SSH Config - Modular configuration
# Original backed up to ~/.ssh/config.bak

# OrbStack
Include ~/.orbstack/ssh/config

# Host-specific configurations (loaded before defaults)
Include ~/.ssh/config.d/10-personal.conf
Include ~/.ssh/config.d/20-company.conf
Include ~/.ssh/config.d/30-internal.conf

# Global defaults (must be last - Host * applies to all)
Include ~/.ssh/config.d/00-defaults.conf
