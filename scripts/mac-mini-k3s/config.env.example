# Mac Mini K3s Worker Node Configuration
# Copy this file to config.env and fill in your values

# =============================================================================
# K3s Cluster Settings
# =============================================================================

# K3s server URL (your existing k3s master)
K3S_URL="https://<YOUR_K3S_SERVER_IP>:6443"

# K3s node token (get from master: sudo cat /var/lib/rancher/k3s/server/node-token)
K3S_TOKEN=""

# =============================================================================
# Node Configuration
# =============================================================================

# Node name (will be used as hostname in VM)
NODE_NAME="mac-mini-worker"

# Environment: dev | prod
NODE_ENV="dev"

# Node purpose label: general | ml | build
NODE_PURPOSE="general"

# =============================================================================
# VM Settings
# =============================================================================

# VM name in OrbStack
VM_NAME="k3s-worker"

# Ubuntu version for VM
UBUNTU_VERSION="22.04"

# VM resources (optional - OrbStack auto-scales by default)
# VM_CPUS="4"
# VM_MEMORY="8G"
# VM_DISK="50G"

# =============================================================================
# Network Settings
# =============================================================================

# Network mode: nat | bridge
# - nat: VM uses OrbStack's NAT (simpler, works out of box)
# - bridge: VM gets IP from your LAN (requires additional setup)
NETWORK_MODE="nat"

# For bridge mode: your network interface (e.g., en0)
# BRIDGE_INTERFACE="en0"

# Static IP for VM (optional, for bridge mode)
# VM_STATIC_IP="<YOUR_VM_STATIC_IP>"

# =============================================================================
# Additional K3s Options
# =============================================================================

# Extra k3s agent flags (optional)
# K3S_EXTRA_ARGS="--node-taint key=value:NoSchedule"

# Skip k3s installation (useful for debugging)
# SKIP_K3S_INSTALL="false"
